validateEmail = (email) => {
    var re = /^\S+@\S+\.\S+$/;
      return re.test(email);
  };
  validatepass = (email) => {
    var re = /^\S+@\S+\.\S+$/;
      return re.test(email);
  };
  validateUserName = (email) => {
    var re = /^\S+@\S+\.\S+$/;
      return re.test(email);
  };
  validatePhoneNumber = (email) => {
    var re = /^\S+@\S+\.\S+$/;
      return re.test(email);
  };



export default  validateEmail ;
