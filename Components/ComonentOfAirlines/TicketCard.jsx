import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

import CardOfTicket from '../ComponentsOfTicket/CardOfTicket'

export default function TicketCard({item,navigation,type}) {
  return (
    <View>
      {/* <CardOfTicket 
      item={item}
      navigation={navigation}
      type={type}
      /> */}
    </View>
  )
}

const styles = StyleSheet.create({})